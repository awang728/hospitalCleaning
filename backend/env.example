# CleanSight Backend — environment variables
# Copy this file to .env and fill in your values
# Never commit .env to git

# ── Sphinx AI ─────────────────────────────────────────────────────────────────
SPHINX_API_KEY=
SPHINX_BASE_URL=https://api.sphinx.ai/v1

# ── Actian VectorAI DB ────────────────────────────────────────────────────────
# If running Docker locally:
VECTORAI_HOST=localhost
VECTORAI_PORT=50051

# If running on same Vultr server as the backend:
# VECTORAI_HOST=127.0.0.1

# ── Server ────────────────────────────────────────────────────────────────────
PORT=5000
